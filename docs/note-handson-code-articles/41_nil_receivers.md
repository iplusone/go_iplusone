# 41 Nil Receivers: `nil` でもメソッド呼び出し自体は書ける

このハンズオンは、Go の少し意外な性質を扱います。ポインタレシーバのメソッドは、対象が `nil` でも呼び出しの形自体は書けます。

もちろん安全に使うには、メソッド内で `nil` チェックが必要です。そこをしないと、実体に触れた瞬間に落ちます。

この性質を知っておくと、後で `nil` を含む設計を読むときに混乱しにくくなります。「呼べる」と「安全に処理できる」は別だと分かるからです。

この回は頻出ではありませんが、Go のポインタとメソッドの関係を少し深く理解するのに役立ちます。
