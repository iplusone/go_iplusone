# 48 Channels Bidirectional

2 本の `channel` を使った双方向のやり取りを確認するためのハンズオンです。

扱う内容:

- main から goroutine へ値を渡す
- goroutine から main へ結果を返す
- 役割ごとに channel を分ける考え方

実行方法:

```bash
go run ./examples/handson/48_channels_bidirectional
```
