# 42 Empty Interface

このハンズオンでは、空のインタフェース `interface{}` が「どんな値でも保持できる」ことを確認します。

## 学べること

- `interface{}` はメソッドを1つも持たないインタフェース
- そのため、整数、文字列、スライス、真偽値などをまとめて保持できる
- 便利だが、取り出して使うときは元の型を意識する必要がある

## 実行方法

```bash
go run ./examples/handson/42_empty_interface
```
