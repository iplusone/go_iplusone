# 15 Infinite Loop Break Continue

無限ループと `continue` `break` を確認するためのハンズオンです。

扱う内容:

- `for {}` による無限ループ
- `break` によるループ終了
- `continue` による次の繰り返しへの移動
- `break` と `continue` の組み合わせ

実行方法:

```bash
go run ./examples/handson/15_infinite_loop_break_continue
```
