# 34 New And Make

`new` と `make` の違いを確認するためのハンズオンです。

扱う内容:

- `new` がポインタを返すこと
- `make` で `slice` を作る
- `make` で `map` を作る
- `make` で `channel` を作る

実行方法:

```bash
go run ./examples/handson/34_new_and_make
```
