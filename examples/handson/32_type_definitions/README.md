# 32 Type Definitions

`type` で型として定義することを確認するためのハンズオンです。

扱う内容:

- `type` による独自型の定義
- `int` や `string` から新しい型を作る
- 構造体のフィールドで独自型を使う
- 元の型とは別の型として扱われること

実行方法:

```bash
go run ./examples/handson/32_type_definitions
```
