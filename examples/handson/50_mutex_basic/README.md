# 50 Mutex Basic

共有データを `sync.Mutex` で保護する基本を確認するためのハンズオンです。

扱う内容:

- 構造体に共有データと `Mutex` を持たせる
- `Lock` と `Unlock`
- ロック中だけ共有データを更新する考え方

実行方法:

```bash
go run ./examples/handson/50_mutex_basic
```
