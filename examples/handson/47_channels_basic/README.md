# 47 Channels Basic

`channel` の基本的な送信と受信を確認するためのハンズオンです。

扱う内容:

- `make(chan T)` で channel を作る
- `c <- value` で値を送る
- `<-c` で値を受け取る
- channel が同期にも使えること

実行方法:

```bash
go run ./examples/handson/47_channels_basic
```
