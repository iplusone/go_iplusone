# 05 Short Variable Declaration

暗黙的型宣言を確認するためのハンズオンです。

扱う内容:

- `:=` による暗黙的型宣言
- 値から型が決まるしくみ
- 暗黙的型宣言した変数への再代入
- 複数の変数の同時宣言

実行方法:

```bash
go run ./examples/handson/05_short_variable_declaration
```
