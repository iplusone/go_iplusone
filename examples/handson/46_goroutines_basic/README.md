# 46 Goroutines Basic

`goroutine` の基本を確認するためのハンズオンです。

扱う内容:

- `go` を付けた関数呼び出し
- main と goroutine が並行に進むこと
- 実行順が固定されないこと
- 終了待ちに `sync.WaitGroup` を使うこと

実行方法:

```bash
go run ./examples/handson/46_goroutines_basic
```
