# 18 Slice Append And Operations

`append` による要素追加と、スライスの基本操作を確認するためのハンズオンです。

扱う内容:

- `append` で1つ要素を追加する
- `append` で複数要素を追加する
- スライスの一部を取り出す
- スライスの要素を書き換える
- `...` を使って別スライスを結合する

実行方法:

```bash
go run ./examples/handson/18_slice_append_and_operations
```
