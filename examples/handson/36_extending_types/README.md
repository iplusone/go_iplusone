# 36 Extending Types

既存の型を拡張する考え方を確認するためのハンズオンです。

扱う内容:

- `type` で既存型を元に独自型を作る
- 独自型にメソッドを追加する
- `string` を元にした型の拡張
- `slice` を元にした型の拡張

実行方法:

```bash
go run ./examples/handson/36_extending_types
```
