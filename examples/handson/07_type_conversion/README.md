# 07 Type Conversion

値のキャスト、型変換を確認するためのハンズオンです。

扱う内容:

- `int` から `float64` への変換
- `float64` から `int` への変換
- 計算のために型をそろえる書き方
- 数値から文字への変換
- 独自型への変換

実行方法:

```bash
go run ./examples/handson/07_type_conversion
```
