# 10 Imports

`import` を確認するためのハンズオンです。

扱う内容:

- `import` の基本
- `fmt` パッケージの利用
- `strings` パッケージの利用
- `strconv` パッケージの利用
- 使っていない import がエラーになること

実行方法:

```bash
go run ./examples/handson/10_imports
```
