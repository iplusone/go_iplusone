# 17 Arrays And Slices

配列 `array`、`range`、`slice`、スライスの長さと容量を確認するためのハンズオンです。

扱う内容:

- 配列 `array`
- 配列に対する `range`
- スライス `slice`
- `append`
- スライスの `len`
- スライスの `cap`

実行方法:

```bash
go run ./examples/handson/17_arrays_and_slices
```
